# GitHub公開前に確認

このフォルダーは「プリン買いにいこ！」バージョン4の公開用ソースです。

## 含まれるもの

- ゲーム本体のソースコード
- 背景・キャラクターなど、ゲームが読み込む画像
- スマホ・キーボード入力、難易度、ゲーム進行のテスト
- `package-lock.json` を含む依存関係の情報
- 開発・ビルド方法を記載した `README.md`

## 除外したもの

- `node_modules`、ビルド結果、キャッシュ
- ローカル環境変数と認証情報
- 元の非公開Sitesプロジェクトに結び付いた `.openai/hosting.json`
- 生成済みの `tsconfig.tsbuildinfo`

## 公開前に決めること

この配布物にはライセンスを追加していません。第三者によるコードや画像の利用を許可する場合は、GitHubで公開する前に方針に合う `LICENSE` を追加してください。

人物の元資料は制作者提供です。背景とキャラクター素材には生成画像が含まれます。公開範囲や再利用条件は、リポジトリ所有者が確認してください。

## GitHubへ載せる手順

1. このフォルダーの中身を新しいGitHubリポジトリへアップロードします。
2. ローカルで実行する場合は Node.js 22.13以上で `npm install` を実行します。
3. `npm test` でテストし、`npm run dev` で開発画面を開きます。
4. 公開用ファイルは `npm run build` で生成します。

ZIPをGitHubのリポジトリ画面へそのまま置くのではなく、ZIPを展開して、中にあるファイルとフォルダーをアップロードしてください。
